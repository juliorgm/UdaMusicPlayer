<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="main_titulos">
        <item name="android:color">@color/white</item>
        <item name="android:textSize">@dimen/size_musica_titulo</item>
        <item name="android:background">@color/colorPrimaryDark</item>
        <item name="android:padding">@dimen/size_padding_padrao</item>
        <item name="android:textAlignment">center</item>
    </style>

    <style name="main_lista_horizontal">
        <item name="android:layout_height">@dimen/main_size_lista_horizontal</item>
    </style>

</resources>
